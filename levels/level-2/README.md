# Level 2 - The Library

Future level folder. Add level-specific HTML, CSS, JavaScript, question data, images, and audio here.

Keep shared systems such as authentication, saved progress, settings, scoring rules, and common utilities outside this folder so they can be reused by every level.
